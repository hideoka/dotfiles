# dotfiles

## isntall requirement
- neovim
- pyenv
- pyenv-vertualenv
- tmux
- ripgrep
_ ctags

## Synbolic link
```
ln -s ~/.dotfiles/.vim/nvim ~/.config/nvim
ln -s ~/.dotfiles/.tmux.conf ~/.tmux.conf
ln -s ~/.dotfiles/.ctags.d ~/.ctags.d
```
